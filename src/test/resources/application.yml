server:
  port: 8081

spring:
  profiles:
    active: test

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100 # IN 쿼리의 개수

logging:
  level:
    org.hibernate.SQL: trace
